import { Coordinates } from './coordinates';

export class City {

  id: number; // City ID
  name: string; // City name
  coord: Coordinates; // City coordinates
  country: string; // Country code (BE, GB, JP, etc.)

}
